[project]
name = "job-terminal-tui"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "rich>=15.0.0",
]

[build-system]
requires = ["uv_build>=0.9.27,<0.10.0"]
build-backend = "uv_build"
