[project]
name = "pipeline"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "job-terminal-models",
    "typer>=0.25.1",
]

[tool.uv.sources]
job-terminal-models = { workspace = true }
