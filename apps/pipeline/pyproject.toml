[project]
name = "pipeline"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "job-terminal-models",
    "markdown-it-py>=4.2.0",
    "resend>=2.30.1",
    "rich>=15.0.0",
    "typer>=0.25.1",
]

[tool.uv.sources]
job-terminal-models = { workspace = true }
