[project]
name = "job-terminal-models"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "sqlmodel>=0.0.38",
]

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"
